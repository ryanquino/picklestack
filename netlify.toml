[build]
  command = "npm ci && npm run build --workspace=client"
  publish = "client/dist"

[build.environment]
  # Set VITE_API_URL in Netlify site settings to your deployed server URL
  # Example: https://picklestack-server.onrender.com/api
  NODE_VERSION = "22"
