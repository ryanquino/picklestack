services:
  - type: web
    name: picklestack-server
    runtime: node
    buildCommand: npm install && npm rebuild better-sqlite3
    startCommand: node --import tsx server/src/index.ts
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "22"
